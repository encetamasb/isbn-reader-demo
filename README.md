Run the following commands to set it up

    # generating .env file with uid and gid
    make env

    # pulling and building images
    docker-compose pull
    docker-compose build


If all went well then you can start it up:

    docker-compose up
